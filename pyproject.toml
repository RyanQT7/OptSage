[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "optsage"
version = "0.1.0"
description = "Agentic explainable reasoning for optical-network failure localization"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["optsage*"]
